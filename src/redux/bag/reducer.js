const initialState = {
    products: [],
}

const bagReducer = (state=initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default bagReducer;